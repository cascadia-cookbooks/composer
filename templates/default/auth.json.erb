{
<% if node['composer']['auth']['http-basic'] %>
    "http-basic": {
<% node['composer']['auth']['http-basic'].each do |host,data| -%>
        "<%= host %>": {
            "username": "<%= data['user'] %>",
            "password": "<%= data['pass'] %>"
        }
<% end -%>
<% if node['composer']['auth']['github-oauth'] %>
    },
<% else %>
    }
<% end %>
<% end %>
<% if node['composer']['auth']['github-oauth'] %>
    "github-oauth": {
<% node['composer']['auth']['github-oauth'].each do |host,token| -%>
        "<%= host %>": "<%= token %>"
<% end -%>
    }
<% end %>
}
